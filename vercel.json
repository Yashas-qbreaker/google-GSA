{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/multiprompt", "dest": "/multiprompt.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
